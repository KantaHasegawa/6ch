import { Box, Typography } from '@mui/material';

const Footer = () => {
  return (
    <Box
      sx={{
        marginTop: 'auto',
        padding: '2rem 10rem',
        textAlign: "center",
      }}
    >
      <Typography sx={{ marginTop: '1rem' }}>@6channel</Typography>
    </Box>
  );
};

export default Footer;
